apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: clone-runtime-repo-to-workspace
spec:
  workspaces:
    - name: openwhisk-workspace
  inputs:
    resources:
      - name: runtime-git
        type: git
        targetPath: runtime
  steps:
    - name: clone-runtime-repo-to-workspace
      image: ubuntu
      script: |
        #!/usr/bin/env bash
        set -xe
        cp -avr $(inputs.resources.runtime-git.path)/ $(workspaces.openwhisk-workspace.path)/
